import { providers } from 'ethers';
import { Chain } from '../components/RainbowKitProvider/RainbowKitChainContext';
import { ApiProvider } from './apiProviders/ApiProvider';
export declare const configureChains: <Provider extends providers.BaseProvider = providers.BaseProvider, WebSocketProvider extends providers.WebSocketProvider = providers.WebSocketProvider>(defaultChains: Chain[], apiProviders: ApiProvider<Provider, WebSocketProvider>[]) => {
    chains: Chain[];
    provider: ({ chainId }: {
        chainId?: number | undefined;
    }) => Provider;
    webSocketProvider: ({ chainId }: {
        chainId?: number | undefined;
    }) => WebSocketProvider;
};
